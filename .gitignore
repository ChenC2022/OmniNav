node_modules
dist
.wrangler
public/omninav-extension.zip
.dev.vars
*.log
.DS_Store
*.tsbuildinfo

# 开发文档（仅本地保留，不随仓库公开）
# docs/ 目录已纳入版本控制

# 删库重建一次性清单（仅本地使用）
REBUILD_REPO_CHECKLIST.md

# Cursor / Agent 本地配置（不参与 Git 同步）
.agent
.agent/
.agents
.agents/
.cursor
.cursor/
skills-lock.json
